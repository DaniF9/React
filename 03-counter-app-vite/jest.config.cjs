module.exports = {
    //TODO: jsdom,
    testEnvironment: 'jest-environment-jsdom', // esta se agrego por el error del render seguido a ello se instalos la dependencia environment
    setupFiles:['./jest.setup.js']
}